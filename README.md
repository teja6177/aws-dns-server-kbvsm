# AWS DNS Server for kbvsm.com

This repository contains an Ansible playbook and setup script to:

1. Launch an AWS Linux EC2 instance
2. Install BIND DNS server
3. Configure `kbvsm.com` as a private DNS domain

Usage: run the playbook in AWS CloudShell.
