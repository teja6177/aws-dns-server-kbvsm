---
- name: Launch DNS EC2 instance with security group
  hosts: localhost
  connection: local
  gather_facts: no
  vars:
    domain_name: kbvsm.com
    region: ap-south-1
    key_name: YOUR_KEY_NAME       # Replace with your existing EC2 key
    vpc_id: ""                    # optional, default VPC if empty
    sg_name: dns-sg
    ami_id: ami-0f58b397bc5c1f2e8  # Replace with your desired CentOS AMI
    instance_type: t2.micro

  tasks:

    - name: Create DNS security group
      amazon.aws.ec2_group:
        name: "{{ sg_name }}"
        description: Security group for DNS server
        vpc_id: "{{ vpc_id if vpc_id != '' else omit }}"
        region: "{{ region }}"
        rules:
          - proto: tcp
            from_port: 22
            to_port: 22
            cidr_ip: 0.0.0.0/0      # SSH from anywhere
          - proto: tcp
            from_port: 53
            to_port: 53
            cidr_ip: 0.0.0.0/0      # DNS TCP
          - proto: udp
            from_port: 53
            to_port: 53
            cidr_ip: 0.0.0.0/0      # DNS UDP
        rules_egress:
          - proto: -1
            cidr_ip: 0.0.0.0/0
        state: present
      register: dns_sg

    - name: Launch EC2 instance
      amazon.aws.ec2_instance:
        name: dns-server
        instance_type: "{{ instance_type }}"
        image_id: "{{ ami_id }}"
        key_name: "{{ key_name }}"
        region: "{{ region }}"
        security_group: "{{ sg_name }}"
        wait: yes
      register: ec2_output

    - name: Add EC2 instance to dynamic inventory
      add_host:
        name: "{{ ec2_output.instances[0].public_ip_address }}"
        groups: dns
        ansible_user: ec2-user
        ansible_ssh_private_key_file: key.pem

    - name: Show instance info
      debug:
        msg: |
          EC2 instance launched successfully!
          Public IP: {{ ec2_output.instances[0].public_ip_address }}
          Private IP: {{ ec2_output.instances[0].private_ip_address }}
          Instance ID: {{ ec2_output.instances[0].id }}
